检查订单、点击触点、合作方和退款表的主键、时间字段与关联关系。
读取结算规则，取得归因窗口、可付款状态、落地域名、反作弊阈值、档位费率和裁决顺序。
使用SQLite关联同一用户的订单与先前点击，按规则顺序写入触点排除表。
用窗口函数排列合格触点，为每个订单选择最后一次点击并标记被替代候选。
汇总退款得到订单净额，依合作方档位计算整数分佣金并保留未归因订单。
从佣金明细按合作方汇总订单、净额、佣金和订单清单。
导出三张业务报表，核对业务主键、字段顺序与SQLite派生表。
